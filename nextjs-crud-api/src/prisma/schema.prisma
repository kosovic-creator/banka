datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Stanje {
  id Int @id @default(autoincrement())
  korisnik String
  stanje Float
  kredit Float?
  isplata Float?
  uplate Float?
}